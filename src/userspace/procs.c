#include <stdint.h>

#include "sys.h"

void main() {
    //for (int i = 0; i < 1; i++)
    // Sometimes get a new prompt; sometimes don't...  Memory use stays low either way, so I think it's killed correctly either way...
        print("Procs...\n");
}
